import * as React from "react";
import * as ReactDOM from "react-dom";
import Router from "./router/inidex"
import "./base.less"
ReactDOM.render(<Router />, document.getElementById("app") as Element);
